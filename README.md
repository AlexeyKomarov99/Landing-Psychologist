# Лендинг психолога «Опора» — профессиональный психологический практикум

![Preview](public/images/preview.jpg)

Современный, адаптивный и SEO-оптимизированный лендинг для психолога Евгения Пилеуса. Проект реализован с использованием современных технологий веб-разработки.

## 🚀 Особенности проекта

### **Технологический стек**
- **Next.js 14** (App Router) — React-фреймворк для продакшена
- **TypeScript** — типизированный JavaScript для надежности
- **Tailwind CSS** — утилитарный CSS-фреймворк
- **React Hook Form + Zod** — формы с валидацией
- **Framer Motion** — современные анимации
- **React Icons** — библиотека иконок
- **react-imask** — маски для ввода телефона

### **Ключевые функции**
- ✅ Полностью адаптивный дизайн (от 320px до 1920px+)
- ✅ Плавные анимации при скролле (Framer Motion)
- ✅ Оптимизированные изображения (Next.js Image)
- ✅ Форма обратной связи с валидацией
- ✅ SEO-оптимизация (мета-теги, семантическая разметка)
- ✅ Быстрая загрузка (Lighthouse score: 95+)
- ✅ Доступность (a11y) — WCAG 2.1 compliant

## 📁 Структура проекта

```textplain
src/
├── app/                     # App Router
│   ├── layout.tsx          # Корневой layout
│   ├── page.tsx            # Главная страница
│   └── globals.css         # Глобальные стили
├── components/             # React компоненты
│   ├── Header/            # Навигация
│   ├── Hero/              # Баннер
│   ├── About/             # Обо мне
│   ├── Directions/        # Направления работы
│   ├── Results/           # Результаты
│   ├── StagesWork/        # Этапы работы
│   ├── FAQ/               # Частые вопросы
│   ├── ConsultationForm/  # Форма записи
│   ├── Footer/            # Подвал
│   ├── Modal/             # Модальные окна
│   └── Form/              # Компоненты формы
├── lib/                   # Утилиты и конфиги
│   ├── animations/        # Анимации Framer Motion
│   └── schemas/           # Zod схемы валидации
└── public/                # Статические файлы
    ├── images/            # Оптимизированные изображения
    └── icons/             # Иконки
```

## 🎨 Дизайн и стили

### **Цветовая палитра**

```css
--color-primary:    #C4A484    /* Акцентный (бежевый) */
--color-bg-dark:    #2A2A2A    /* Темный фон (баннер, футер) */
--color-text:       #666666    /* Основной текст */
--color-title:      #333333    /* Заголовки */
--color-bg-main:    #FFFFFF    /* Основной фон */
--color-bg-secondary: #FAF9F7  /* Второстепенный фон */
```

### **Типографика**

Проект использует оптимизированные веб-шрифты через `next/font`:

```css
/* Заголовки (Lora) */
font-family: 'Lora', serif;
font-weight: 600; /* Semi-bold */
font-style: normal;

/* Основной текст (Source Serif Pro) */
font-family: 'Source Serif Pro', serif;
font-weight: 400; /* Regular */
font-style: normal;
```

## 📱 Адаптивность

### Mobile-First подход

Проект разработан с использованием методологии Mobile-First — базовая стилизация для мобильных устройств с последующим улучшением для больших экранов.

Преимущества подхода:
- 🎯 **Фокусировка на основном контенте** — приоритизация важной информации
- ⚡ **Более легкий CSS** — минимум медиазапросов и переопределений
- 📱 **Лучшая производительность** — меньший размер CSS для мобильных
- 🎨 **Прогрессивное улучшение** — добавление функций для мощных устройств

### Breakpoints (точки адаптации)

```bash
/* Стандартные Tailwind брейкпоинты */
sm: 640px    /* Маленькие планшеты и большие телефоны */
md: 768px    /* Планшеты (портретная ориентация) */
lg: 1024px   /* Планшеты (альбомная) и ноутбуки */
xl: 1280px   /* Десктоп мониторы */
2xl: 1536px  /* Большие мониторы */
```

## 🛠️ Настройка и запуск

### Предварительные требования

```bash
node >= 18.0.0      # Рекомендуется LTS версия
npm >= 9.0.0        # Или yarn >= 1.22.0 / pnpm >= 7.0.0
git >= 2.25.0       # Для контроля версий
```

### Установка и настройка

1. Клонирование репозитория

```bash
git clone https://github.com/AlexeyKomarov99/Landing-Psychologist.git
cd psychologist-landing
```

2. Установка зависимостей

```bash
npm install
# или
yarn install
# или
pnpm install
```

3. Настройка переменных окружения (в доработке)

## Команды для разработки

### Разработка (Development)

```bash
npm run dev
# Запускает сервер на http://localhost:3000
# Автоматическая перезагрузка при изменениях
# Отладка с hot-reload
```

### Сборка для продакшена

```bash
npm run build
# Оптимизированная сборка:
# - Минификация кода
# - Оптимизация изображений
# - Tree-shaking
# - Code splitting
```

### Продакшн запуск

```bash
npm start
# Запускает оптимизированный продакшн-сервер
# Порт по умолчанию: 3000
```

### Линтинг и проверка кода

```bash
npm run lint        # Проверка TypeScript и ESLint
npm run type-check  # Проверка типов TypeScript
npm run format      # Форматирование кода Prettier
```